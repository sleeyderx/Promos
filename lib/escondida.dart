import 'package:flutter/material.dart';

class EscondidaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Escondida'),
      ),
      body: Center(
        child: Text('Contenido de la página Escondida'),
      ),
    );
  }
}

