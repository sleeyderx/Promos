import 'package:flutter/material.dart';

class BurgerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Burger King'),
      ),
      body: Center(
        child: Text('Contenido de la página Burger King'),
      ),
    );
  }
}

