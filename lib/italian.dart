import 'package:flutter/material.dart';

class ItalianPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('The Italian Coffee'),
      ),
      body: Center(
        child: Text('Contenido de la página The Italian Coffee'),
      ),
    );
  }
}

